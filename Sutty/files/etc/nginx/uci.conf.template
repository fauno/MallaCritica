worker_processes auto;

user root;

events {}

http {
  server_names_hash_bucket_size 128;
  server_tokens off;
  access_log off;
  sendfile on;
  tcp_nopush on;
  tcp_nodelay on;
  charset utf-8;

  include mime.types;
  #UCI_HTTP_CONFIG
  include conf.d/*.conf;
}
